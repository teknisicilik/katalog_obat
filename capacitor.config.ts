import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'katalogobat.teknisicilik.com',
  appName: 'Apotek Jamus',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
